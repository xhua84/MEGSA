% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/megsa.R
\name{megsa_search}
\alias{megsa_search}
\alias{funAdd1}
\alias{funMaxS}
\alias{funMaxSSimu}
\alias{funGlobalTest}
\alias{funAdd1RealData}
\alias{funDrop1RealData}
\alias{funSelect}
\title{Search and test MEGSA gene sets}
\usage{
funAdd1(
  geneStart,
  mutationMat,
  detail = TRUE,
  search = c("screened", "exhaustive"),
  corMat = NULL
)

funMaxS(
  mutationMat,
  nPairStart = 10,
  maxSize = 6,
  detail = TRUE,
  search = c("screened", "exhaustive")
)

funMaxSSimu(
  mutationMat,
  nSimu = 1000,
  nPairStart = 10,
  maxSize = 6,
  detail = TRUE,
  search = c("screened", "exhaustive"),
  ncores = 1,
  seed = NULL
)

funGlobalTest(
  mutationMat,
  maxSSimu = NULL,
  nSimu = 1000,
  nPairStart = 10,
  maxSize = 6,
  detail = TRUE,
  search = c("screened", "exhaustive"),
  ncores = 1,
  seed = NULL
)

funAdd1RealData(
  geneStartList,
  mutationMat,
  maxSSimu,
  eps = 0.005,
  level = 0.05,
  nPerm = 100,
  detail = TRUE
)

funDrop1RealData(
  geneStartList,
  mutationMat,
  maxSSimu,
  eps = 0.005,
  level = 0.05,
  nPerm = 100,
  permResult = list(),
  detail = TRUE
)

funSelect(
  mutationMat,
  maxSSimu = NULL,
  nSimu = 1000,
  nPairStart = 10,
  maxSize = 6,
  level = 0.05,
  detail = TRUE,
  search = c("screened", "exhaustive"),
  ncores = 1,
  seed = NULL
)
}
\arguments{
\item{geneStart}{Character vector of starting gene names.}

\item{mutationMat}{A logical or numeric matrix with samples in rows and
genes in columns. Non-zero numeric values are treated as mutations.}

\item{detail}{If \code{TRUE}, print progress messages.}

\item{search}{Search strategy. \code{"screened"} uses the 2016 correlation
screening heuristic; \code{"exhaustive"} evaluates all candidate pairs and
additions as in the earlier script release.}

\item{corMat}{Optional precomputed correlation matrix used internally by
the screened search strategy.}

\item{nPairStart}{Number of starting gene pairs for the forward search.}

\item{maxSize}{Maximum size of a mutually exclusive gene set.}

\item{nSimu}{Number of simulations to run when \code{maxSSimu} is not supplied.}

\item{ncores}{Number of parallel worker processes for simulations.}

\item{seed}{Optional random seed for simulation reproducibility. Seeded
simulations use the same per-simulation permutations regardless of
\code{ncores}.}

\item{maxSSimu}{Optional null-distribution matrix generated by
\code{\link[=funMaxSSimu]{funMaxSSimu()}}. Rows are simulations and columns correspond to gene-set
sizes 2 through \code{maxSize}.}

\item{geneStartList}{List of gene sets used internally by the selection
procedure.}

\item{eps}{Tolerance used when comparing q-values during model selection.}

\item{level}{Family-wise error rate threshold.}

\item{nPerm}{Number of local permutations for add/drop decisions.}

\item{permResult}{Cached permutation results used internally.}
}
\value{
Search/test functions return lists or matrices matching the
historical MEGSA script outputs.
}
\description{
Search and test MEGSA gene sets
}
